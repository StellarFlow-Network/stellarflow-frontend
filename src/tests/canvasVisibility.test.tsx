describe(
  'canvas rendering',
  () => {
    it(
      'stops rendering when hidden',
      () => {
        // mock intersection observer

        // verify render loop paused
      },
    );
  },
);

it(
  'resumes rendering when visible',
  () => {
    // simulate visibility return
  },
);

it(
  'cancels animation frame on unmount',
  () => {
    // verify RAF cancelled
  },
);